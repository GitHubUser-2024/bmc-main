[package]
name = "bmc-wasm-widgets"
version = "0.0.0"
edition = "2024"
license = "GPL-3.0-or-later"
publish = false

[dependencies]

[lints]
workspace = true

[workspace]
resolver = "3"
members = [
    "bitcoin-mining-data",
    "blockheight",
    "braiins-pool",
    "clock",
    "fleet-management",
    "formula-1",
    "halving-countdown",
    "image",
    "iss-position",
    "lib/miner-info",
    "lib/mining",
    "lib/prices",
    "lib/remote-image",
    "lib/units",
    "miner-info-geek",
    "miner-info-mining",
    "miner-info-overload",
    "mining-clock",
    "nameday",
    "picture-of-the-day",
    "random-facts",
    "spacex-launch",
    "ticker-list",
    "ticker-single",
    "victron-solar",
    "weather",
]

# Shared lints for all production widget crates
[workspace.lints.rust]
unused_must_use = "deny"

[workspace.lints.clippy]
doc_markdown = "allow"
missing_errors_doc = "allow"
missing_panics_doc = "allow"
module_name_repetitions = "allow"
pedantic = { level = "warn", priority = -1 }
unwrap_used = "warn"

[workspace.dependencies]
base64 = { version = "0.22", default-features = false, features = ["alloc"] }
# Serde-free: bincode 2's native `Encode`/`Decode` derive keeps `serde` out of the wasm graph
bincode = { version = "2.0.0", default-features = false, features = ["alloc", "derive"] }
form_urlencoded = { version = "1.2", default-features = false, features = ["alloc"] }
sgp4 = { version = "2.4", default-features = false, features = ["std"] }

# Shared profiles — all widgets are tiny WASM binaries optimized for size
[profile.dev]
opt-level = "z"
lto = true
panic = "abort"

[profile.release]
lto = true
codegen-units = 1
opt-level = "z"
panic = "abort"
strip = true

# Release optimization but keeps the wasm `name` section for profiling tools.
[profile.profiling]
inherits = "release"
strip = false

# Lint only, for a tree of its own: `clippy-driver` leaves one `wasm::dev`
# then fails to link out of. No `lto`, since a lint never links the wasm.
[profile.fast]
inherits = "dev"
lto = false
